package com.example.cardclash.core.settings;

public enum SettingType {
    INT_SLIDER,    // bounded numeric
    INT_PICKER,    // discrete values
    TOGGLE,        // boolean
    ENUM_PICKER,   // one-of strings
    LIST_EDITOR    // ordered list (e.g. variation queue)
}
