<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- hard offset shadow -->
    <item android:left="2dp" android:top="2dp">
        <shape android:shape="rectangle">
            <solid android:color="#000000" />
            <corners android:radius="2dp" />
        </shape>
    </item>
    <!-- card back: deep red w/ black border (45° stripe pattern is approximated) -->
    <item android:right="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="45"
                android:startColor="@color/bal_red_deep"
                android:centerColor="@color/bal_card_black"
                android:endColor="@color/bal_red_deep" />
            <stroke android:width="2dp" android:color="@color/bal_card_black" />
            <corners android:radius="2dp" />
        </shape>
    </item>
</layer-list>
